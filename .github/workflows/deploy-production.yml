name: Deploy Production (Frontend)

on:
  push:
    branches: ["client"] # đổi theo nhánh FE của bạn
  pull_request:
    branches: ["client"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build docker image
        run: docker build --platform linux/amd64 -t caophi562005/pixcam-client:v1 .

      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login --username ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

      - name: Push docker image to DockerHub
        run: docker push caophi562005/pixcam-client:v1

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to production server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST_PRODUCTION }}
          username: ${{ secrets.USERNAME_PRODUCTION }}
          password: ${{ secrets.PASSWORD_PRODUCTION }}
          port: ${{ secrets.PORT_PRODUCTION }}
          script: |
            set -e

            echo "🚀 Bắt đầu triển khai Frontend..."

            PROJECT_DIR="$HOME/pixcam-client"
            mkdir -p "$PROJECT_DIR"
            cd "$PROJECT_DIR"

            echo "📥 Docker login..."
            echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login --username ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

            echo "📦 Pull image mới nhất..."
            docker pull caophi562005/pixcam-client:v1

            echo "🧹 Dọn image rác..."
            docker image prune -f || true

            echo "🔄 Dừng & xóa container cũ (nếu có)..."
            docker stop pixcam-client 2>/dev/null || echo "No container to stop"
            docker rm pixcam-client 2>/dev/null || echo "No container to remove"

            # Tạo .env (nếu bạn muốn biến cấu hình, không bắt buộc cho FE static)
            echo "⚙️ Tạo tệp .env..."
            cat <<'EOF' > .env
            ${{ secrets.ENV_CLIENT }}
            EOF
            echo "✅ Đã tạo tệp .env."

            # Tạo docker-compose.yml
            echo "⚙️ Tạo tệp docker-compose.yml..."
            cat <<'EOF' > docker-compose.yml
            version: '3.8'
            services:
              pixcam-client:
                image: caophi562005/pixcam-client:v1
                container_name: pixcam-pixcam
                restart: unless-stopped
                ports:
                  - "${HOST_PORT:-3100}:80"
            EOF
            echo "✅ Đã tạo tệp docker-compose.yml."

            echo "🚀 Khởi động container bằng Docker Compose..."
            docker compose pull
            docker compose up -d --force-recreate

            echo "🎉 Triển khai Frontend hoàn tất!"
